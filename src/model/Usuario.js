export default class Usuario {

    constructor(
        id,
        nomeCompleto,
        email,
        tipoPerfil
    ) {

        this.id = id;
        this.nomeCompleto = nomeCompleto;
        this.email = email;
        this.tipoPerfil = tipoPerfil;
    }

}